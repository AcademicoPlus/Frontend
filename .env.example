# Copie este arquivo para ".env" e ajuste se a API estiver em outro endereço.
VITE_API_URL=http://localhost:8080/api

# "true" para testar o frontend com dados fictícios (services/mocks.ts) sem
# precisar rodar o academico-backend nem o banco. Veja
# docs/CONECTANDO_FRONT_BACKEND.md, seção "Testando sem o backend".
VITE_USE_MOCKS=false
